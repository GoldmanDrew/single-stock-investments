---
description: AI infrastructure valuation overlay — capex cycle, chips, backlog (Marvin + Milly)
globs: "**/research/**"
alwaysApply: false
---

# AI infrastructure valuation

When researching **AI hyperscalers** (GOOGL, AMZN, META, MSFT) or any ticker with `ai_overlay` in `valuation.json`, read:

**`_system/frameworks/ai_infrastructure_valuation.md`**

## Marvin

1. Set `ai_overlay` in `valuation.json` with `in_model`, `not_in_model_requires_refresh`, `capex_stress`, optional `ai_inflection_bull`.
2. Add **`#### AI infrastructure — model coverage`** in Business & moat (what is / is not in Lawrence math).
3. Keep **Lawrence FCF₀** filing-based; use **capex_stress** for trough-year illustration, not as silent FCF₀.
4. Label press-only claims (TPU sales, JV $, cost-cut %) as **[Assumption]** until SEC text.
5. Pair with `segment_cashflow_valuation.md` when `valuation_overlay: segment_cashflow`.

## Milly

1. Run **valuation staleness** checks (FCF₀ year vs capex guide vs `ai_overlay`).
2. Flag every `not_in_model_requires_refresh` item as **Inference risk** or dive gap.
3. Set YAML `valuation_staleness` and `ai_coverage` per `MILLY.md`.
