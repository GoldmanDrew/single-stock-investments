# Corrections Log

Agent errors and human corrections. **Read before any pipeline or valuation work**
(step 3b of `_system/prompts/_prefix.md`) and before cross-check reports.

These are mistakes already made once. Repeating one is the most expensive kind of
error, because it means the loop is not compounding. When a correction here
contradicts a prompt, a README or your own reasoning, the correction wins — or
supersede it with a new row and say why.

Append a row whenever a run hits a pipeline or tooling error a future run could
repeat: a silent overwrite, a config that shadows another, a file regenerated from
somewhere unexpected. A fix recorded only in a chat transcript is lost.

| Date | Ticker | Error | Correction | Source |
|------|--------|-------|------------|--------|
| 2026-08-07 | — | Edited `{TICKER}/research/security_identity.json` directly to fix an archetype or valuation profile. It reverts on the next readiness run. | `automate_valuation_readiness.py` **regenerates** that file from `_system/reference/security_identity_overrides.json` (`resolve_identity`). Write the durable entry there, then re-run and confirm the identity survives. `_system/reference/valuation_followups.json` `tickers.<T>.method_profile` is a *second*, separate file that `power_zone_router` reads for the explicit profile — set both. | `_system/scripts/automate_valuation_readiness.py:65,1114` |
| 2026-08-07 | — | Fixed a missing `cik` in `_system/portfolio/registry.json` and the SEC download still reported `SEC=0`. | `_system/scripts/us_ticker_config.json` is read **first**; the registry is only consulted when the ticker is absent from it, so an entry with `"cik": null` beats a correct registry value. Update both, and verify with a non-zero `SEC=` count rather than by re-reading the registry. | `_system/scripts/download_us_investor_docs.py` |
| 2026-08-07 | — | Ran `automate_valuation_readiness.py` over a ticker with accumulated analysis and silently lost it. | It can collapse a rich `research/valuation.json` to a compiler skeleton (observed 34 keys → 9, dropping `scenarios`, `synthesis`, `stance_proposal`, `human_review`) while still reporting `decision_grade`. Snapshot `valuation.json` first and diff the top-level key set after. Losses limited to `context_overlay` / `human_review` / `insider_signal` / `notes` are fine — the daily refresh regenerates those. | observed on STHO 2026-08-06; TBBK 2026-08-07 |
| 2026-08-07 | WHK | Treated Cash Available for Distribution as a pre-interest cash flow and subtracted net debt from a CAFD-derived value. | CAFD is defined and reconciled **after** cash interest expense, cash taxes and cash preferred dividends, so it is already an equity-level cash flow. Subtracting net debt or preferred again double-counts them — the `scarce_asset_optionality` profile's second listed failure mode, "operating cash flow and NAV counted twice". Always read the issuer's own non-GAAP definition before using the measure. | WHK 424B4, "Non-GAAP Financial Measures" |
| 2026-08-07 | WHK | Valued an Up-C issuer on consolidated cash flow without applying the public company's economic interest. | In an Up-C the listed company owns a *fraction* of the operating LLC (WHK: 86.0% of WhiteHawk OpCo; Continuing Equity Owners hold 14.0% exchangeable 1:1 into Class A). Apply the economic interest **before** dividing per share, or the minority's share is counted as shareholder value — the profile's first failure mode, "gross asset value mistaken for shareholder value". Class B carries votes and no economic rights and is not a claim on value. | WHK 424B4, "Our Organizational Structure" |
| 2026-08-07 | — | Added a validator check that read `{ticker}/research/*.json` and it silently failed for every ticker in CI. | `validate_dashboard_data.py` runs in the sparse `pages` checkout, which has **no ticker trees**. Anything the validator needs must be carried in the payload it validates (see `valuation_decision.extreme_return_validated`, stamped by `build_dashboard_data`). | `_system/scripts/ci_dashboard_deploy_mode.sh` |
| 2026-08-07 | — | Assumed committing research changes updates the live Cloudflare dashboard. | Every deploy path resolves to `deploy-only`: Pages publishes the committed `dashboard/` verbatim and never rebuilds. The SPA boots from `core.json` plus per-ticker shards, not `dashboard_data.json`. A validator ERROR fails the build step and the deploy step is **skipped silently**. Rebuild and commit the artifacts, and check the deploy actually ran. | `_system/scripts/ci_dashboard_deploy_mode.sh`, run 31198948792 |
| 2026-08-07 | — | Read a green calibration bar as evidence the capability worked. | `locator_accuracy` returned exactly `1.000 MEETS BAR` on **zero** adjudicated cases, and `severity5_recall` returned 100% on a single event. Both now report INSUFFICIENT DATA. A bar with no adjudications behind it is not a measurement — check the detail line, not the verdict. | `_system/scripts/calibrate_ssi.py` |
| 2026-08-09 | — | Renamed a workflow ("Research quality (PR)" → "Research quality") and its dependents died silently. | `workflow_run` triggers and `gh run list --workflow` filters match on the workflow **name string**; a rename leaves them referencing nothing, with no error anywhere — automerge re-trigger and ci-autofix coverage were dead for two days. When renaming a workflow, grep `.github/workflows/` for the old name. | `marvin-pr-automerge.yml:18,181`, `ci-autofix.yml:15` |
| 2026-08-09 | — | Assumed the 2-hourly backfill refill would recover from failed agent jobs. | The refill only pushes/dispatches when the rebuilt wave **changed**, and failed tickers stay `evidence_blocked`, so 3 failed tickers rebuilt the identical wave forever — a 644-deep queue frozen 4 days inside green runs. `build_contract_backfill_queue.py` now rotates a stalled wave and parks tickers after `MAX_DISPATCH_ATTEMPTS`; check `stall_breaker`/`dispatch_attempts` in the queue JSON. Lane freshness (last landed commit), not run conclusions, is the health signal. | `_system/scripts/build_contract_backfill_queue.py` |
| 2026-08-09 | NVO | Treated "no locked `shares_outstanding` fact" as a data-collection problem and looked for a filing to download. | The share selector only knew `dei:EntityCommonStockSharesOutstanding` and two us-gaap weighted tags, plus a `ifrs-full:WeightedAverageNumberOfSharesOutstanding` that **is not a real IFRS element**. An IFRS foreign private issuer therefore matched nothing and dead-ended permanently no matter how many times collection re-ran. The selector now also reads `ifrs-full:NumberOfSharesOutstanding` (point-in-time) and `ifrs-full:AdjustedWeightedAverageShares` / `WeightedAverageShares` (IAS 33 diluted/basic), and falls back to summing the classes on the 20-F/40-F cover item as a last resort. When a fact is "missing" for a non-US filer, check the taxonomy the issuer actually files in before blaming collection. | `_system/scripts/automate_valuation_readiness.py` `ENTITY_SHARE_SPECS` / `_cover_page_share_count` |
| 2026-08-09 | AAOI/ADBE | Froze a committee packet by hashing the **live** research files, so every daily compiler write aged the packet and `select_committee_work` superseded it (AAOI: 25 archives, merged rater votes discarded; ADBE deadlocked behind an assembled record built from a superseded packet). | `initialize()` now copies evidence into `committee_work/<date>/evidence_snapshot/` and hashes the **copies** (copy-on-freeze), so live churn cannot age a packet; supersede needs a real reason (`refresh_requested`, corrupt copies, legacy drift). Votes must carry `evidence_hash` equal to the packet (`validate_vote`, and the PR gate). After `MAX_REFRESHES_WITHOUT_VOTES=3` re-freezes with no vote landing the packet is parked into `_system/data/committee_triage.json` instead of looping — prior supersede archives count toward that budget. An assembled `committee_{date}.json` only hides live work when its `packet_hash` matches the manifest. | `_system/scripts/investment_committee_pipeline.py`, `_system/scripts/select_committee_work.py`, `_system/scripts/test_committee_livelock.py` |
| 2026-08-09 | AAOI/ADBE | Wrote the vote-preservation guard as `if landed and not has_snapshot(manifest)`, so it only protected pre-copy-on-freeze packets. Every new packet was unguarded: with votes landed, `refresh_requested` or a missing/edited frozen copy fell through to supersede, which renames the work dir and re-initializes an empty one — the exact AAOI vote loss, now silent. | `select_committee_work.refresh` parks on **any** landed vote regardless of packet generation, and the `refresh_limit` breaker no longer carries its own `and not landed` gate (votes are handled first). When a guard is written for a legacy shape, check whether the new shape needs it more, not less. | `_system/scripts/select_committee_work.py`, `_system/scripts/test_committee_livelock.py` |
| 2026-08-09 | — | Auto-closed 616 onboard checklists on checks that discriminated nothing: `onboard_checks` tested the **truthiness** of `archetype`/`moat`/`dhando`/`stance`/`cycle`, but the onboarding defaults (`unknown`, `unproven`, `pending`, `watch`, `-`) are all truthy, the `-` no-sleeve sentinel passed the sleeve check, and an empty `ir_roots` emitted a `no_ir_root_asserted` **pass**. 584 of the 616 had all five fields still at defaults. | A checklist that asks a human to *confirm defaults* cannot be closed by a test the defaults satisfy. The gate now compares each field against `portfolio_registry.DEFAULT_CLASSIFICATION` plus a `PLACEHOLDER_VALUES` set, requires a non-sentinel sleeve, and treats an empty `ir_roots` as `ir_roots_missing`. `--reverify-closed` re-runs the current gate over `auto_closed/` and re-opens failures (append-only ledger, `"action": "reopened"`). When adding a check, assert it can fail on the data actually in the registry — count how many rows it rejects before trusting it. | `_system/scripts/build_review_queue_rollup.py`, `_system/reviews/QUEUE.md` |
| 2026-08-09 | ASML | Corrected the fact ledger to USD and called the currency bug closed. `valuation.json` still carried the raw EUR figures labelled `"USD millions"` (cash 12916.0, debt 2709.0, owner earnings 11084.9) mixed with a USD price, and `scan_fact_ledger_currency.py` reported green because it only globbed `*/research/valuation_fact_ledger.json` — a validator green on data it never looked at. The ledger is *not* the file the model reads. Refresh the proof inputs too (`refresh_valuation_proof_inputs.py --tickers ...`, the narrow call that will not collapse a rich `valuation.json`), then re-run `run_security_decision_pipeline.py` so the contract trace stops carrying the stale numbers. The scan now covers both surfaces. | `_system/scripts/refresh_valuation_proof_inputs.py`, `_system/scripts/scan_fact_ledger_currency.py` |
| 2026-08-09 | ASML/NVO | Copied converted ledger facts into `calculation_proof` without the `fx_conversion` block, so a converted value and a raw foreign-currency value both read as plain `"USD millions"` sourced to a EUR/DKK concept. That is exactly why ASML's staleness was not self-evident by inspection. | `_proof_fact` and the `evaluate_calculation_proof` trace builder now carry `fx_conversion` through into the proof and the contract. Anything that copies a ledger fact forward must carry its conversion evidence with it; a bare number plus a USD label is not provenance. | `_system/scripts/automate_valuation_readiness.py` `_proof_fact`; `_system/scripts/calculation_proof.py` |
| 2026-08-09 | — | Wrote an FX rate source string that cited two `api.frankfurter.dev` queries as if they corroborated each other. They are one service asked in two directions — an arithmetic self-check, not a second provider. Say "single provider" and name the independent check that was actually attempted (here `data-api.ecb.europa.eu`, HTTP 503 at retrieval time). Reworded rate text only reaches a ledger/proof on rebuild, so rebuild the ledger and refresh the proofs after editing. | `_system/reference/market-data/fx_rates.json` |
| 2026-08-09 | NVO | `build_fact_ledger` locked foreign-currency companyfacts values (NVO: DKK) as `"USD millions"` — a ~6.4x overstatement waiting for the contract to unblock. ASML (EUR) and UROY (CAD) had the same corruption. | The writer now reads the companyfacts **unit key**: USD passes through, a non-USD currency requires an evidenced rate in `_system/reference/market-data/fx_rates.json` (recorded on the fact as `fx_conversion`), and anything else refuses to lock with an ASCII `REFUSED to lock` message, leaving the fact unlocked (fail closed). Audit any ledger with `scan_fact_ledger_currency.py`; the fact ledger is regenerated by the readiness run, so never hand-edit it — fix the writer or the FX reference. | `_system/scripts/automate_valuation_readiness.py` `_usd_conversion`; `_system/scripts/scan_fact_ledger_currency.py` |
| 2026-08-09 | — | Re-ticked a row in the memory triage queue to reverse a recorded decision; the run printed `0 new` and exited 0. `_system/memory/triage_ledger.json` is **append-only** — a decided id is never overwritten — so the reversal was silently discarded and was indistinguishable from a duplicate tick. | `record()` now warns `[warn] … NOT applied` when the new decision disagrees with the recorded one, and `--mark` counts those refusals and **exits non-zero** so a caller cannot read a refused reversal as success (`--ingest` already reported them). To actually re-decide, delete the id and re-run with `--previous-decision '{…}'`, which stores the destroyed row as a structured `previous_decision`; the ledger is untracked by git, so prose in `reason` was the only history there had ever been. Same lesson one surface over: a `memory_anchor` of just `_system/memory/MEMORY.md` is not a check — every belief satisfies it, so `--audit-promoted` short-circuited on the field merely being *set* and deleting a belief outright left the orphan count unchanged. An anchor must double-quote a phrase that is in the file; a bare path now falls through to the text match it would otherwise have had. A check that cannot fail is not a check. | `_system/scripts/build_memory_triage.py` `record` / `audit_promoted`; `_system/scripts/tests/test_memory_triage.py` |
| 2026-08-09 | — | Ran `build_memory_triage.py --sync-promoted-from-memory --by agent` and stamped 21 **May-2026 human** promotions as `by: "agent"`. The back-sync makes no judgement — it observes that a belief is already in MEMORY.md and back-marks the proposal — but it inherited `--by` from the CLI like every other path, so the human owner's work was attributed to whoever happened to run the sync. "Reverse the agent's decisions wholesale", the stated escape hatch for an agent promotion pass, would have unwound it. | That path now stamps a constant `BACKFILL_BY = "backfill"` and ignores `--by` entirely, and the 21 rows were re-stamped. When a code path *observes* a prior decision rather than making one, it must not inherit the actor flag of the run that noticed it — attribution follows who decided, not who executed. | `_system/scripts/build_memory_triage.py` `BACKFILL_BY`; `_system/memory/triage_ledger.json` |
| 2026-08-09 | AXTI/AXON | Adjudicated an extreme IRR as economically "real" after testing a ladder of book-value and revenue anchors, without checking whether a capital event fell between the balance-sheet date and the share count. AXT closed a $600.2M net follow-on on 2026-04-22; every anchor divided pre-raise equity by the post-raise cover-page count and was wrong by ~1.18x on the share count alone. Correcting the one cash fact inside AXT's own component method moved the base from $1.7432/sh (-40.40%) to $11.6656/sh (-21.81%) — an "artifact", not a finding. | The fact ledger takes `shares_outstanding` from `dei:EntityCommonStockSharesOutstanding` at the **filing cover date** and cash/debt/equity at the **balance-sheet date**. Those are different dates by construction on every 10-Q, so any issuance, buyback or debt raise in between enters the denominator and never the numerator — and it survives any fix that only re-dates the balance sheet, because the mismatch is inside a single per-share division. Before adjudicating any per-share number, reconcile the cover-page count against `us-gaap:CommonStockSharesOutstanding` at the balance-sheet date; if they differ materially read the Subsequent Events note and the 424B5 (AXTI reconciles to the share off the prospectus base of 55,578,827, not off the thousands-rounded 55,579,000 XBRL tag). Separately, `cash_m` never includes `us-gaap:ShortTermInvestments` or `MarketableSecurities`, so an apparent cash collapse may be a reclassification (AXTI cash 128.4 → 57.9 while STI went 0 → 65.4) and the omitted asset can exceed the fact that was locked. | `_system/scripts/automate_valuation_readiness.py` `build_fact_ledger` / `companyfact_specs['cash_m','debt_m']` / `ENTITY_SHARE_SPECS` / `_select_share_companyfact`; `_system/research/extreme_irr_adjudication_2026-08-09.json` XC-6, XC-8 |
| 2026-08-10 | CI | Locked debt from `LongTermDebtNoncurrent` dated 2018-12-31 (**$39.5B**) while FY2025 10-K reports `LongTermDebtAndCapitalLeaseObligations` **$30.9B**, blocking the universal contract with a stale-fact error. | Add `LongTermDebtAndCapitalLeaseObligations` to the `debt_m` companyfact tag list in `automate_valuation_readiness.py` **before** `LongTermDebtNoncurrent`. Rebuild the fact ledger via `marvin_cloud_refresh.py` and confirm the contract blocker clears. | CI contract backfill run 2026-08-10; `_system/scripts/automate_valuation_readiness.py` `companyfact_specs['debt_m']` | // pragma: allowlist secret
| 2026-08-11 | — | Kept two hand-synced copies of the persona independence-group map; the committee pipeline's copy was missing munger and lawrence and its `.get(persona, persona)` fallback minted private single-member groups, so 31 committees seated two quality_reinvestment raters while passing the three-distinct-groups check (23 still active, incl. 8697.T at committee_complete_decision_pending). | The map now lives in `persona_groups.py` and both `power_zone_router.py` and `investment_committee_pipeline.py` import it; `valuation_method_router.ALL_PERSONAS` derives from the same registry (lawrence/moi were never silenceable before). Invariant L6 asserts the registries stay equal, no script re-defines a GROUPS literal, and no active manifest's raters collapse below the quorum under the canonical map. Re-seating/parking the 23 collided committees is a human gate (lens-plane proposal). A constant two scripts must agree on is a constant one module must own. | `_system/scripts/persona_groups.py`; `_system/scripts/graph_invariants.py` `inv_l6`; `_system/scripts/test_graph_invariants.py` |
| 2026-08-11 | WHK | Regenerated lenses.json only inside full marvin refreshes, so every other valuation.json rewrite (readiness compiler, proof-input refresh, manual re-underwrites like WHK 2026-08-11) stranded the lens plane: 510 valued tickers had no lenses.json, 31 stale, 128/192 decision_grade tickers missing-or-stale, and the nightly dashboard baked the stale files. The persona reader also looked for payoff_lens at top level only, hiding 130 classifications (41 asset names) from the personas built for them, and contract-first valuations left every persona 'pending' because the legacy results fields it read were empty. | `persona_lens.py --all --skip-valuation` now runs in the power-zone-universe lane before the dashboard-row refresh and `*/research/lenses.json` is committed with the authority artifacts; `extract_shared_context` reads the layered classification (top-level -> classification_inputs -> registry) and falls back to the contract's `annualized_return_at_price_pct` when the legacy results are empty. Invariants L1/L3 count the remaining debt under the baseline ratchet. A derived artifact with no writer in any lane is a stale artifact with a delay. | `.github/workflows/power-zone-universe.yml`; `_system/scripts/persona_lens_common.py`; `_system/scripts/graph_invariants.py` `inv_l1`/`inv_l3` |
