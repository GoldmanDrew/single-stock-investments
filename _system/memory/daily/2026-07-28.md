
## LS-algo local onboard continue (2026-07-28)

- Registered 24 orphan scaffolds (OSCR→RKT) that had folders but were missing from `registry.json`.
- Scaffolded remaining 52 pending names (SATS→ZETA) plus RMBS earlier in session: skip-download / skip-indexes-per-ticker / no deep dive; sleeve tagged `ls_algo_underlying`.
- Rebuilt gap: **274/274 registered, 0 pending**.
- Ran once: `sync_portfolio_from_registry.py`, `build_folder_indexes.py`, `build_dashboard_data.py` → **830 holdings** in dashboard.
- Evidence still thin on new names (`evidence_blocked` / 0 docs) — next step is download/evidence recovery batches, not more scaffolds.

### [PROPOSED]

- [PROPOSED COMPANY] LS-algo equity universe intake complete locally as of 2026-07-28 (gap pending 0). Downloads and proof-first contracts remain backlog.

## LS-algo download + evidence pass (2026-07-28)

- Downloaded SEC packs for today's scaffolds: **63 with filings**, 3 empty (QNT, SKHY, SPY), 10 no-CIK ETF/non-issuer skips (SOXX, SPHB, STLR, TLT, URA, XBI, XLE, XLF, XLK, XOP) + RMBS already had files.
- Fixed stale INDEX short-circuit: rebuilt INDEX + `build_filing_evidence` + valuation readiness for **64** tickers with docs (now inventoring 10-K/10-Q/8-K HTML).
- Most remain `evidence_blocked` with partial ready counts — expected until companyfacts/model inputs fill.
- Report: `_system/data/ls_algo_download_batch_2026-07-28.json`

### [PROPOSED]

- [PROPOSED] After SEC download for scaffolded names, always rebuild `INDEX.csv` before `build_filing_evidence` (stale INDEX causes docs=0).

## LS-algo download batch 2 started

- Next 30 with CIK + empty sec-edgar: AI → DNN (INDEX + filing evidence inline).
- Remaining after this: ~48 with CIK still empty + ~75 no-CIK (many ETFs; some issuers need map fill).

## LS-algo download batch 2 finished

- Summary in `_system/data/ls_algo_download_batch_2026-07-28_b2.json`.

## Dashboard vs-price / IRR consistency + URC display (2026-07-28)

- **Root cause of UROY `+72613505%`:** `shares_outstanding` stored as millions (361.037) while `total_value_m` math expected absolute shares → multi-million $/sh. Fixed to 361,037,249; added `resolve_share_count` guard + absurd-upside suppress (>50k%).
- **VS PRICE vs IRR:** same dual — IRR = `(value/price)^(1/horizon)-1`. UI now shows total gap with `≈X%/yr · Ny` subtitle so GYRO (+103% / 26.7% / 3y) reads consistently with ADBE (+179% / 15.8% / 7y).
- **URC:** Sweetwater closed 2026-07-27; display ticker **URC**, quote **UROY** (NASDAQ tape still UROY). Folder remains `UROY/` for path stability.

### [PROPOSED]

- [PROPOSED COMPANY] New URC (display URC / quote UROY): post-close base component value ~`$2.16` vs `~$2.96` → ~`-27%` to base until evidence gaps close; prior mega-% was a share-unit bug not economics.
