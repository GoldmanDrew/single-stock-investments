{
  "name": "single-stock-dashboard-cloudflare",
  "private": true,
  "version": "1.0.0",
  "description": "Pinned Cloudflare deployment tooling for the single-stock dashboard.",
  "devDependencies": {
    "wrangler": "4.115.0"
  },
  "scripts": {
    "wrangler": "wrangler",
    "check": "wrangler --version && node --test test-*.mjs"
  }
}
